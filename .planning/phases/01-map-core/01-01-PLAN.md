---
phase: 01-map-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/map/MapView.tsx
  - src/features/map/MapControls.tsx
  - src/features/map/MapErrorBoundary.tsx
  - src/features/map/index.ts
  - src/hooks/useGeolocation.ts
  - src/hooks/useGeolocation.test.ts
  - src/router.tsx
autonomous: true
requirements: [MAP-01, MAP-02, MAP-04, MAP-05, MAP-06, MAP-07]

must_haves:
  truths:
    - "User opens app and sees Mapbox Outdoors map centered on Poland (zoom 6)"
    - "User taps geolocation button and map flies to GPS position at zoom 12"
    - "When GPS is denied, Polish toast appears and search bar briefly highlights"
    - "Switching tabs and returning to Mapa does not produce black screen or WebGL errors"
    - "If WebGL is unsupported or Mapbox token invalid, recovery UI appears instead of crash"
    - "Map viewport syncs to Zustand store on moveend"
  artifacts:
    - path: "src/features/map/MapView.tsx"
      provides: "Map container with useRef lifecycle and moveend viewport sync"
      min_lines: 60
    - path: "src/features/map/MapControls.tsx"
      provides: "Floating geolocation button bottom-right above tab bar"
      min_lines: 30
    - path: "src/features/map/MapErrorBoundary.tsx"
      provides: "Error boundary wrapping MapView with fallback UI"
      min_lines: 25
    - path: "src/hooks/useGeolocation.ts"
      provides: "Geolocation hook with loading/success/error states"
      min_lines: 25
    - path: "src/hooks/useGeolocation.test.ts"
      provides: "Unit tests for useGeolocation hook"
      min_lines: 30
  key_links:
    - from: "src/features/map/MapView.tsx"
      to: "useViewportStore"
      via: "moveend event handler calling setCenter/setZoom/setBounds"
      pattern: "useViewportStore.*set(Center|Zoom|Bounds)"
    - from: "src/features/map/MapControls.tsx"
      to: "src/hooks/useGeolocation.ts"
      via: "locate() call on button tap, flyTo on success"
      pattern: "useGeolocation.*locate"
    - from: "src/router.tsx"
      to: "src/features/map/MapView.tsx"
      via: "index route element replacement"
      pattern: "MapView"
---

<objective>
Build the core interactive Mapbox map with correct WebGL lifecycle, geolocation button, viewport sync to Zustand, and error boundary. This is the foundation every subsequent phase renders on top of.

Purpose: Everything in the app (trails, filters, favorites) depends on a working map with stable WebGL context. Getting lifecycle right from day one prevents impossible-to-retrofit bugs.
Output: Working map page with geolocation, error handling, and viewport store wired.
</objective>

<execution_context>
@/Users/kacpermajerowicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kacpermajerowicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-map-core/01-CONTEXT.md
@.planning/phases/01-map-core/01-RESEARCH.md

Skills to read before execution:
- .agents/skills/frontend-design/SKILL.md
- .agents/skills/responsive-design/SKILL.md
- .agents/skills/tailwind-design-system/SKILL.md
- .agents/skills/vercel-react-best-practices/SKILL.md

<interfaces>
<!-- Existing Zustand store the executor must wire to -->

From src/stores/viewport.ts:
```typescript
interface ViewportState {
  center: [number, number]
  zoom: number
  bounds: { north: number; south: number; east: number; west: number } | null
  setCenter: (center: [number, number]) => void
  setZoom: (zoom: number) => void
  setBounds: (bounds: ViewportState['bounds']) => void
}
export const useViewportStore: UseBoundStore<StoreApi<ViewportState>>
```

From src/stores/ui.ts:
```typescript
interface UIState {
  viewMode: 'map' | 'list'
  isFilterOpen: boolean
  setViewMode: (mode: ViewMode) => void
  toggleFilter: () => void
  setFilterOpen: (open: boolean) => void
}
export const useUIStore: UseBoundStore<StoreApi<UIState>>
```

From src/components/layout/AppLayout.tsx:
```typescript
// Flex column layout: <main className="flex-1 overflow-hidden flex flex-col"> wraps <Outlet />
// MapView must fill this flex container: use "relative w-full h-full" on outer div, "absolute inset-0" on map container
```

From src/router.tsx:
```typescript
// Current MapPage stub at index route:
// const MapPage = () => <div>Mapa</div>
// Replace with: { index: true, element: <MapErrorBoundary><MapView /></MapErrorBoundary> }
```

From src/index.css (design tokens):
```css
--color-bg-base: #111318;
--color-bg-surface: #1C1F26;
--color-bg-elevated: #252930;
--color-accent: #C9A84C;
--color-text-primary: #F5F5F5;
--color-text-secondary: #A0A0A0;
--color-text-muted: #666666;
--color-error: #EF4444;
--spacing-tab-bar: 4.5rem;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useGeolocation hook with tests</name>
  <files>src/hooks/useGeolocation.ts, src/hooks/useGeolocation.test.ts</files>
  <behavior>
    - Test: initial state is { status: 'idle' }
    - Test: calling locate() sets status to 'loading'
    - Test: successful geolocation sets status to 'success' with position coords
    - Test: permission denied (code 1) sets status to 'error' with code 1
    - Test: timeout (code 3) sets status to 'error' with code 3
    - Test: missing navigator.geolocation sets status to 'error' with code 0
  </behavior>
  <action>
    Create `src/hooks/useGeolocation.ts`:
    - Export type `GeolocationState` as discriminated union: `{ status: 'idle' }` | `{ status: 'loading' }` | `{ status: 'success'; position: GeolocationPosition }` | `{ status: 'error'; code: number; message: string }`
    - Export `useGeolocation()` hook returning `{ state: GeolocationState; locate: () => void }`
    - `locate()` calls `navigator.geolocation.getCurrentPosition()` with options: `{ enableHighAccuracy: false, timeout: 10000, maximumAge: 60000 }`
    - On missing `navigator.geolocation`: error with code 0, message "Brak obslugi geolokalizacji"
    - Use `useCallback` for `locate` to keep stable reference

    Create `src/hooks/useGeolocation.test.ts`:
    - Mock `navigator.geolocation.getCurrentPosition` to test all state transitions
    - Use `vi.fn()` to capture success/error callbacks from getCurrentPosition
    - Test each behavior listed above
    - Use `@testing-library/react` `renderHook` and `act` for hook testing (install `@testing-library/react` if not present — check package.json first)

    Update `vitest.config.ts` if needed: include pattern must match `.test.ts` files (already configured).
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && npx vitest run src/hooks/useGeolocation.test.ts</automated>
  </verify>
  <done>useGeolocation hook passes all 6 test cases covering idle, loading, success, denied, timeout, and unsupported states</done>
</task>

<task type="auto">
  <name>Task 2: MapView with lifecycle, viewport sync, error boundary, and router wiring</name>
  <files>src/features/map/MapView.tsx, src/features/map/MapErrorBoundary.tsx, src/features/map/index.ts, src/router.tsx</files>
  <action>
    **Create `src/features/map/MapView.tsx`:**
    - Import `mapbox-gl` and `mapbox-gl/dist/mapbox-gl.css`
    - Set `mapboxgl.accessToken = import.meta.env.VITE_MAPBOX_TOKEN`
    - Two refs: `containerRef = useRef<HTMLDivElement>(null)` and `mapRef = useRef<mapboxgl.Map | null>(null)`
    - Export `mapRef` pattern: accept `onMapReady?: (map: mapboxgl.Map) => void` prop so child overlays can access the map. Alternatively, use a simpler approach: pass `mapRef` directly to child components as a prop (`mapRef: React.RefObject<mapboxgl.Map | null>`)
    - `useEffect` with initialization:
      1. Guard: `if (mapRef.current) return` (Strict Mode double-init prevention per MAP-06)
      2. Guard: `if (!containerRef.current) return`
      3. Pre-check: `if (!mapboxgl.supported()) throw new Error('WebGL2 not supported')` — this will trigger MapErrorBoundary
      4. Create map: `new mapboxgl.Map({ container, style: 'mapbox://styles/mapbox/outdoors-v12', center: [19.145, 51.919], zoom: 6, attributionControl: false })`
      5. Add compact attribution: `map.addControl(new mapboxgl.AttributionControl({ compact: true }), 'bottom-right')`
      6. Wire `moveend` handler per MAP-04: read `map.getCenter()`, `map.getBounds()`, `map.getZoom()` and call `setCenter`, `setZoom`, `setBounds` from `useViewportStore`
      7. Cleanup: `mapRef.current?.remove(); mapRef.current = null`
    - Render: outer `<div className="relative w-full h-full">` containing map container `<div ref={containerRef} className="absolute inset-0" />`, plus `<MapControls mapRef={mapRef} />` (LocationSearch added in Plan 02)
    - Do NOT include LocationSearch yet — Plan 02 adds it

    **Create `src/features/map/MapErrorBoundary.tsx`** (MAP-07):
    - React class component (error boundaries must be class components as of React 19)
    - `getDerivedStateFromError` sets `{ hasError: true }`
    - `componentDidCatch` logs error to console
    - Fallback UI: centered div with `bg-bg-surface` background, error icon (lucide-react `AlertTriangle`), Polish heading "Nie udalo sie zaladowac mapy", Polish description "Sprawdz polaczenie internetowe i odswierz strone", and a "Sprobuj ponownie" button that calls `window.location.reload()`
    - Style with design tokens: `bg-bg-surface`, `text-text-primary`, `text-accent` for button
    - Also handle the case where `!mapboxgl.supported()` — before throwing in useEffect, render a similar fallback. The simplest approach: in MapView, check `mapboxgl.supported()` at render time and conditionally render an inline fallback, OR throw so the boundary catches it. Use the throw approach so MapErrorBoundary handles both runtime errors and WebGL unsupported.

    **Create `src/features/map/index.ts`:**
    - Re-export `MapView` from `./MapView`
    - Re-export `MapErrorBoundary` from `./MapErrorBoundary`
    - Re-export `MapControls` from `./MapControls`

    **Update `src/router.tsx`:**
    - Import `MapView` and `MapErrorBoundary` from `./features/map`
    - Replace the stub `MapPage` with: `{ index: true, element: <MapErrorBoundary><MapView /></MapErrorBoundary> }`
    - Keep all other routes as stubs

    **Vite worker bundling validation:** After implementation, run `npm run dev` and confirm map renders. If worker errors appear in console, add `optimizeDeps: { exclude: ['mapbox-gl'] }` to `vite.config.ts` as documented in RESEARCH.md Pitfall 7.
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && npm run build</automated>
  </verify>
  <done>Map renders on app load centered on Poland (zoom 6) with compact attribution. Switching tabs and returning shows map without black screen. TypeScript compilation passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: MapControls geolocation button with flyTo and toast feedback</name>
  <files>src/features/map/MapControls.tsx</files>
  <action>
    **Create `src/features/map/MapControls.tsx`:**

    Props: `{ mapRef: React.RefObject<mapboxgl.Map | null> }`

    **Geolocation button (per locked decisions):**
    - Floating circular button, bottom-right, above the tab bar
    - Position: `absolute bottom-[calc(var(--spacing-tab-bar)+1rem)] right-4` (places it above the 4.5rem tab bar with 1rem gap)
    - Size: `w-12 h-12` (48px touch target per design system)
    - Style: `bg-bg-surface` background, `text-text-primary` icon, rounded-full, shadow-lg
    - Icon: `LocateFixed` from lucide-react (or `Navigation2` — use `LocateFixed` as it matches the "locate me" metaphor)
    - Wait — the button is inside MapView's relative container, but the tab bar is OUTSIDE (in AppLayout). So `bottom-0` of the map container aligns with the top of the tab bar. Use `absolute bottom-4 right-4` (16px from the bottom of the map area, which is just above the tab bar).

    **Button states and behavior:**
    - Idle: static icon
    - Loading (GPS acquiring): pulsing animation on the button (`animate-pulse` class). Per locked decision: "Pulsing animation on button while GPS is being acquired"
    - On tap: call `locate()` from `useGeolocation` hook
    - On success: call `mapRef.current?.flyTo({ center: [lng, lat], zoom: 12, duration: 1500, essential: true })`. Wait for `map.once('load')` — actually, flyTo does not require load event if map is already loaded. Check `mapRef.current?.loaded()` before flyTo. If not loaded, use `mapRef.current?.once('load', () => flyTo(...))`.
    - On GPS denied (error code 1): show toast "Brak dostepu do lokalizacji" (per locked decision). For toast: since there is no Toast component yet, create a minimal toast using a temporary div with `position: fixed`, `bottom: calc(var(--spacing-tab-bar) + 4rem)`, auto-dismiss after 3 seconds. Use `useState` + `setTimeout` pattern. Style: `bg-bg-elevated text-text-primary` with rounded corners and shadow. Alternatively, if the plan is too large, use `window.alert()` as placeholder — NO, use a proper inline toast. Keep it simple: a `<div>` that renders conditionally with fade-in/fade-out.
    - On GPS timeout (error code 2 or 3): toast "Nie udalo sie znalezc lokalizacji" and stop pulsing
    - Per locked decision: "Geolocation button stays active after denial — user can tap to retry"

    **Toast implementation (inline, minimal):**
    - Manage toast state: `{ message: string; visible: boolean } | null`
    - Show toast: set message + visible, setTimeout 3s to hide
    - Render at bottom of MapControls: `fixed bottom-[calc(var(--spacing-tab-bar)+5rem)] left-1/2 -translate-x-1/2` with `bg-bg-elevated text-text-primary text-sm px-4 py-2 rounded-lg shadow-lg` and `animate-[fade-in_200ms]`
    - This toast is a local implementation for Phase 1. A shared toast system can be extracted later if needed.

    **Search bar highlight on GPS denial:**
    - Per locked decision: "briefly pulse/highlight the search bar" on GPS denied
    - Since LocationSearch is not yet built (Plan 02), skip search bar highlighting for now. Plan 02 will wire this interaction. Add a comment: `// TODO: Plan 02 — highlight search bar on GPS denial`

    **Integration with MapView:**
    - MapView renders `<MapControls mapRef={mapRef} />` inside the relative container
    - MapControls is an overlay that does not affect map container sizing
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && npm run build</automated>
  </verify>
  <done>Geolocation button is visible bottom-right above tab bar. Tapping it acquires GPS and flies map to user location at zoom 12. GPS denial shows Polish toast. Button pulses during GPS acquisition. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no TypeScript errors
2. `npx vitest run` passes (useGeolocation tests green)
3. `npm run dev` shows map centered on Poland at zoom 6
4. Clicking geolocation button triggers GPS prompt (or flies to location if already granted)
5. Tab navigation (Mapa -> Trasy -> Mapa) does not produce black screen
6. Viewport store updates visible in React DevTools after panning
</verification>

<success_criteria>
- Interactive Mapbox Outdoors map renders on app load at Poland center (zoom 6)
- Geolocation button bottom-right flies map to GPS position at zoom 12
- GPS denial shows Polish toast "Brak dostepu do lokalizacji"
- Tab navigation preserves map rendering (no WebGL context leaks)
- Error boundary shows recovery UI on WebGL/token failure
- Viewport store syncs on moveend
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-core/01-01-SUMMARY.md`
</output>
