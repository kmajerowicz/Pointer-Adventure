---
phase: 04-filters
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/features/map/FilterPanel.tsx
  - src/features/map/FilterButton.tsx
  - src/features/map/ActiveFilterChips.tsx
  - src/features/map/MapView.tsx
  - src/features/trails/TrailList.tsx
  - src/index.css
autonomous: false
requirements: [FILT-01, FILT-08, FILT-10]

must_haves:
  truths:
    - "Filter button opens a bottom sheet with all 6 filter categories and a sticky Zastosuj button"
    - "Applied filters appear as dismissible chips in a horizontal bar below the search bar"
    - "Filter button shows a count badge when filters are active"
    - "Tapping Zastosuj updates trail list and map pins instantly (client-side)"
    - "Wyczysc wszystko clears all filters and restores full trail list"
    - "Closing sheet without Zastosuj discards draft changes"
    - "Filters affect BOTH map pins AND trail list"
  artifacts:
    - path: "src/features/map/FilterPanel.tsx"
      provides: "Bottom sheet with draft state, 6 filter sections, sticky apply button"
      min_lines: 100
    - path: "src/features/map/FilterButton.tsx"
      provides: "Floating circular trigger with active count badge"
      min_lines: 15
    - path: "src/features/map/ActiveFilterChips.tsx"
      provides: "Horizontal scrolling chip bar with dismiss buttons"
      min_lines: 30
  key_links:
    - from: "src/features/map/MapView.tsx"
      to: "src/hooks/useFilteredRoutes.ts"
      via: "useFilteredRoutes() replaces direct routes usage for updateTrailData"
      pattern: "useFilteredRoutes"
    - from: "src/features/trails/TrailList.tsx"
      to: "src/hooks/useFilteredRoutes.ts"
      via: "useFilteredRoutes() replaces useTrailsStore routes"
      pattern: "useFilteredRoutes"
    - from: "src/features/map/FilterPanel.tsx"
      to: "src/stores/filters.ts"
      via: "Draft state committed to store on Zastosuj tap"
      pattern: "useFiltersStore"
    - from: "src/features/map/FilterButton.tsx"
      to: "src/hooks/useActiveFilterCount.ts"
      via: "Badge count from useActiveFilterCount()"
      pattern: "useActiveFilterCount"
---

<objective>
Build the FilterPanel bottom sheet, FilterButton trigger, ActiveFilterChips bar, and wire useFilteredRoutes into MapView and TrailList so filters affect both views.

Purpose: The UI layer for Phase 4 — users can open a bottom sheet, select filters using pill toggle groups and a toggle switch, apply them with a single tap, and see results instantly on both map and list.
Output: Complete filter UI integrated into the app. Filters work end-to-end.
</objective>

<execution_context>
@/Users/kacpermajerowicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kacpermajerowicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-filters/04-CONTEXT.md
@.planning/phases/04-filters/04-RESEARCH.md
@.planning/phases/04-filters/04-01-SUMMARY.md

@src/features/map/MapView.tsx
@src/features/map/MapControls.tsx
@src/features/map/LocationSearch.tsx
@src/features/trails/TrailList.tsx
@src/stores/filters.ts
@src/stores/ui.ts
@src/index.css

<interfaces>
<!-- From Plan 04-01 outputs -->

From src/hooks/useFilteredRoutes.ts:
```typescript
export function useFilteredRoutes(): Route[]
```

From src/hooks/useActiveFilterCount.ts:
```typescript
export function useActiveFilterCount(): number
```

From src/features/map/filterLabels.ts:
```typescript
export const SURFACE_LABELS: Record<SurfaceType, string>
export const DIFFICULTY_LABELS: Record<Difficulty, string>
export const LENGTH_OPTIONS: { label: string; value: 'short' | 'medium' | 'long' }[]
export const DISTANCE_OPTIONS: { label: string; value: 10 | 30 | 50 }[]
export const WATER_OPTIONS: { label: string; value: 'required' | 'preferred' | 'any' }[]
```

From src/stores/filters.ts:
```typescript
// defaults: length=null, surface=null, water='any', difficulty=null, distance=null, marked=null
export const useFiltersStore: {
  length, surface, water, difficulty, distance, marked,
  setLength, setSurface, setWater, setDifficulty, setDistance, setMarked, resetAll
}
```

From src/stores/ui.ts:
```typescript
export const useUIStore: { isFilterOpen, setFilterOpen, toggleFilter, viewMode, setViewMode }
```

<!-- Existing UI patterns -->

MapControls geolocation button pattern:
```tsx
<button className="absolute bottom-4 right-4 w-12 h-12 rounded-full bg-bg-surface text-text-primary shadow-lg flex items-center justify-center active:scale-95 transition-transform">
```

LocationSearch container:
```tsx
<div ref={containerRef} className="absolute top-4 left-4 right-4 z-10">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterPanel, FilterButton, ActiveFilterChips, CSS keyframes</name>
  <files>src/features/map/FilterPanel.tsx, src/features/map/FilterButton.tsx, src/features/map/ActiveFilterChips.tsx, src/index.css</files>
  <action>
    **FilterPanel.tsx** — Bottom sheet with draft state pattern:
    - Props: `{ isOpen: boolean; onClose: () => void; scrollToCategory?: string | null }`
    - Draft state: `useState` copy of all 6 filter values, re-initialized from store when `isOpen` becomes true (useEffect on isOpen)
    - Backdrop: `fixed inset-0 z-40 bg-black/60`, onClick calls onClose (discards draft)
    - Sheet: `fixed bottom-0 left-0 right-0 z-50 bg-bg-surface rounded-t-2xl max-h-[85vh]` with sheet-up animation
    - Drag handle: centered `w-10 h-1 rounded-full bg-bg-elevated` bar at top
    - Scrollable content area with `overflow-y-auto` containing 6 filter sections:
      1. Dlugosci (Length) — PillGroup with LENGTH_OPTIONS from filterLabels
      2. Nawierzchnia (Surface) — PillGroup built from SURFACE_LABELS entries
      3. Dostep do wody (Water) — PillGroup with WATER_OPTIONS; subtitle hint "Trasy z woda wyzej" under "Mile widziana" option
      4. Trudnosc (Difficulty) — PillGroup built from DIFFICULTY_LABELS entries
      5. Odleglosc (Distance) — PillGroup with DISTANCE_OPTIONS; show "Wymaga lokalizacji" hint if GPS not available
      6. Szlak oznaczony (Marked) — ToggleSwitch component (role="switch", aria-checked)
    - Section refs for scroll-to-category support (useRef Record)
    - On scrollToCategory prop change, scrollIntoView the matching section
    - "Wyczysc wszystko" reset link at top of scrollable area — resets draft to defaults (FILT-10)
    - Sticky footer: "Zastosuj" button (`w-full h-12 rounded-xl bg-accent text-bg-base font-semibold`) — commits draft to store via setLength/setSurface/etc, then calls onClose
    - Internal PillGroup component: horizontal flex-wrap gap-2, pills are `px-4 py-2 rounded-full text-sm min-h-[2.75rem]` with border; selected = `bg-accent text-bg-base border-accent`; unselected = `bg-transparent text-text-secondary border-bg-elevated`; tap toggles (same value = deselect to null)
    - Internal ToggleSwitch: `w-11 h-6 rounded-full` with sliding `w-5 h-5` circle; checked = bg-accent; uses role="switch" + aria-checked
    - Exit animation: use isClosing state flag, animate sheet-down 200ms, then unmount

    **FilterButton.tsx** — Floating trigger:
    - Props: `{ onPress: () => void }`
    - `absolute bottom-4 left-4 w-12 h-12 rounded-full bg-bg-surface text-text-primary shadow-lg` (mirrors MapControls geolocation button at bottom-right)
    - Icon: SlidersHorizontal from lucide-react (size=20, strokeWidth=1.75)
    - Badge: useActiveFilterCount() — if > 0, show `absolute -top-1 -right-1 w-5 h-5 rounded-full bg-accent text-bg-base text-[0.6rem] font-bold` with count
    - aria-label: "Filtry tras" + active count

    **ActiveFilterChips.tsx** — Chip bar:
    - Props: `{ onChipTap: (category: string) => void }`
    - Reads useFiltersStore to build chip array: each non-default filter becomes a chip with { key, label (value-only format), category, remove fn }
    - Label format: value only (e.g., "< 5 km", "Ziemia", "Latwa") using filterLabels
    - Renders only when chips.length > 0
    - Container: `flex gap-2 overflow-x-auto` with hidden scrollbar CSS (`[&::-webkit-scrollbar]:hidden [-ms-overflow-style:none] [scrollbar-width:none]`)
    - Each chip: `shrink-0 bg-bg-elevated border border-bg-elevated rounded-full pl-3 pr-2 py-1.5 text-xs text-text-primary`
    - Chip body (label) onClick calls onChipTap(category) — opens filter panel scrolled to that category
    - X button calls remove fn (sets that filter to default via store setter)
    - X icon: lucide-react X, size=12

    **src/index.css** — Add keyframes inside @theme block:
    - `sheet-up`: from translateY(100%) to translateY(0)
    - `sheet-down`: from translateY(0) to translateY(100%)

    Per user decision: all labels in Polish. Bottom sheet half-height with drag handle. Pill toggle groups for all multi-option filters. Toggle switch for marked filter. Chip bar below search bar with horizontal scroll. Filter button at bottom-left.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>FilterPanel renders all 6 filter sections with correct Polish labels, draft state pattern, sticky Zastosuj button, and Wyczysc wszystko reset. FilterButton shows at bottom-left with badge. ActiveFilterChips renders dismissible chips with horizontal scroll. Sheet animations work. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useFilteredRoutes into MapView and TrailList</name>
  <files>src/features/map/MapView.tsx, src/features/trails/TrailList.tsx</files>
  <action>
    **MapView.tsx** modifications:
    - Import useFilteredRoutes from '../../hooks/useFilteredRoutes'
    - Import FilterButton from './FilterButton'
    - Import FilterPanel from './FilterPanel'
    - Import ActiveFilterChips from './ActiveFilterChips'
    - Import useUIStore from '../../stores/ui'
    - Replace `const routes = useTrailsStore((s) => s.routes)` with `const filteredRoutes = useFilteredRoutes()`
    - Update the trail data useEffect to use `filteredRoutes` instead of `routes`: `updateTrailData(map, filteredRoutes)`
    - Add state for filter panel: `const isFilterOpen = useUIStore((s) => s.isFilterOpen)` and `const setFilterOpen = useUIStore((s) => s.setFilterOpen)`
    - Add scrollToCategory state: `const [scrollToCategory, setScrollToCategory] = useState<string | null>(null)`
    - Render FilterButton with onPress={() => setFilterOpen(true)} — placed after MapControls in JSX
    - Render ActiveFilterChips as a separate `absolute top-[4.25rem] left-4 right-4 z-10` div in MapView JSX, after LocationSearch. The 4.25rem offset = 1rem (top-4) + 3.25rem (search bar approximate height). If this doesn't align perfectly, adjust to `top-[4.5rem]` based on visual inspection.
    - Render FilterPanel with isOpen={isFilterOpen}, onClose={() => { setFilterOpen(false); setScrollToCategory(null) }}, scrollToCategory={scrollToCategory}
    - handleChipTap: setScrollToCategory(category), setFilterOpen(true)
    - Keep the existing `useTrails()` call for loading/error/retry — it still triggers on bounds change. Only the trail DATA passed to updateTrailData changes.

    **TrailList.tsx** modifications:
    - Import useFilteredRoutes from '../../hooks/useFilteredRoutes'
    - Replace `const routes = useTrailsStore((s) => s.routes)` with `const routes = useFilteredRoutes()`
    - The sorting logic already uses `routes` variable, so just changing the source is sufficient
    - Empty state check remains on `routes.length === 0` — this now means "no trails match filters" which is the correct UX

    IMPORTANT anti-patterns to avoid:
    - Do NOT add filter state as a dependency to useTrails hook (FILT-09)
    - Do NOT use position:fixed for chip bar — must be position:absolute inside map container
    - Do NOT store draft state in Zustand — only FilterPanel local state
    - Do NOT mutate the routes array — always spread before sort
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>MapView renders FilterButton (bottom-left), ActiveFilterChips (below search), and FilterPanel (bottom sheet). Selecting filters and tapping Zastosuj updates both map pins and trail list. Closing without Zastosuj discards changes. Chip X removes individual filter. Build passes with no TS errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete filter system</name>
  <files>n/a</files>
  <what-built>Complete filter system: bottom sheet with 6 filter categories (pill groups + toggle switch), floating filter button with badge, chip bar, client-side filtering on both map and list views</what-built>
  <action>Human verifies the filter UI works end-to-end by following the steps below.</action>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Verify filter button appears at bottom-left of map (opposite geolocation button at bottom-right)
    3. Tap filter button — bottom sheet slides up with all 6 categories and sticky "Zastosuj" button
    4. Select "< 5 km" length pill — it highlights in accent gold
    5. Select "Ziemia" surface pill
    6. Tap "Zastosuj" — sheet closes, chip bar appears below search with two chips ("< 5 km" and "Ziemia"), filter button shows badge "2"
    7. Verify trail list (Trasy tab) shows only matching trails
    8. Verify map pins also show only matching trails
    9. Tap X on a chip — that filter is removed, trails update
    10. Tap a chip body (label text) — filter panel opens scrolled to that category
    11. Open filter panel, change filters, close by tapping dark backdrop — verify changes are discarded (chips unchanged)
    12. Open filter panel, tap "Wyczysc wszystko" then "Zastosuj" — all filters cleared, full trail list restored
    13. Verify "Szlak oznaczony" uses a toggle switch (not pills)
    14. Verify water filter "Mile widziana" shows hint text below it
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All 14 verification steps pass. Filter system works end-to-end on both map and list views.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npm run build
npx vitest run
</verification>

<success_criteria>
- FilterPanel opens as bottom sheet with all 6 filter categories in Polish
- Pill toggle groups for length, surface, water, difficulty, distance; toggle switch for marked
- Draft state pattern: changes discarded on backdrop close, committed on Zastosuj
- FilterButton at bottom-left with active count badge
- ActiveFilterChips below search bar with horizontal scroll and dismiss buttons
- Chip tap opens panel scrolled to that category
- Filters affect both map pins and trail list simultaneously
- "Wyczysc wszystko" clears all filters (FILT-10)
- Water "Mile widziana" hint text visible
- No new npm packages installed
- Build passes, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-filters/04-02-SUMMARY.md`
</output>
