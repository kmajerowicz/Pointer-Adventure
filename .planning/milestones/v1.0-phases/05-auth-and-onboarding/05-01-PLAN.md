---
phase: 05-auth-and-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/auth.ts
  - src/lib/types.ts
  - supabase/migrations/20260314000000_auth_support.sql
  - supabase/functions/validate-invite/index.ts
  - vitest.config.ts
  - src/stores/auth.test.ts
  - src/features/auth/validateInvite.test.ts
  - src/features/auth/InvitePage.test.tsx
  - src/features/auth/RegisterForm.test.tsx
  - src/features/auth/AuthPage.test.tsx
  - src/components/ui/BottomTabBar.test.tsx
  - src/features/onboarding/OnboardingFlow.test.tsx
  - src/features/onboarding/DogStep.test.tsx
  - src/features/onboarding/GeolocationStep.test.tsx
autonomous: true
requirements: [AUTH-01, AUTH-03, AUTH-06, AUTH-07]

must_haves:
  truths:
    - "Any component can read the current user session and profile without prop drilling"
    - "validate-invite Edge Function returns valid/invalid/expired/used for any token"
    - "New auth.users row auto-creates a public.users row via DB trigger"
    - "consume_invite RPC marks token as used only when called explicitly"
    - "All 10 Wave 0 test stubs exist and pass with --passWithNoTests"
  artifacts:
    - path: "src/stores/auth.ts"
      provides: "Zustand auth store with session, user, profile, loading"
      exports: ["useAuthStore"]
    - path: "supabase/functions/validate-invite/index.ts"
      provides: "Edge Function for server-side invite token validation"
    - path: "supabase/migrations/20260314000000_auth_support.sql"
      provides: "walk_preferences column, handle_new_user trigger, consume_invite function"
    - path: "vitest.config.ts"
      provides: "Updated test include pattern for .tsx files"
    - path: "src/stores/auth.test.ts"
      provides: "Wave 0 test scaffold for auth store"
    - path: "src/features/auth/InvitePage.test.tsx"
      provides: "Wave 0 test scaffold for InvitePage"
    - path: "src/features/auth/RegisterForm.test.tsx"
      provides: "Wave 0 test scaffold for RegisterForm"
    - path: "src/features/auth/AuthPage.test.tsx"
      provides: "Wave 0 test scaffold for AuthPage"
    - path: "src/features/auth/validateInvite.test.ts"
      provides: "Wave 0 test scaffold for validate-invite logic"
    - path: "src/components/ui/BottomTabBar.test.tsx"
      provides: "Wave 0 test scaffold for BottomTabBar auth interception"
    - path: "src/features/onboarding/OnboardingFlow.test.tsx"
      provides: "Wave 0 test scaffold for onboarding flow"
    - path: "src/features/onboarding/DogStep.test.tsx"
      provides: "Wave 0 test scaffold for DogStep"
    - path: "src/features/onboarding/GeolocationStep.test.tsx"
      provides: "Wave 0 test scaffold for GeolocationStep"
  key_links:
    - from: "src/stores/auth.ts"
      to: "src/lib/supabase.ts"
      via: "supabase.auth.getSession + onAuthStateChange"
      pattern: "supabase\\.auth\\.(getSession|onAuthStateChange)"
    - from: "supabase/functions/validate-invite/index.ts"
      to: "public.invitations"
      via: "service role client query"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Auth foundation: Zustand auth store, validate-invite Edge Function, DB migration for auth support (walk_preferences, handle_new_user trigger, consume_invite RPC), vitest config fix, and Wave 0 test scaffolding for all phase 5 features.

Purpose: Establishes the backend and state management foundation that all auth/onboarding UI depends on, plus creates all test file stubs so every subsequent plan task can run per-feature vitest commands as verification. Without the auth store, no component can check session state. Without the Edge Function, invite tokens cannot be validated. Without the trigger, new users have no public.users row.

Output: Working auth store, deployable Edge Function, migration SQL, vitest ready for .tsx tests, 10 Wave 0 test stubs.
</objective>

<execution_context>
@/Users/kacpermajerowicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kacpermajerowicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-auth-and-onboarding/05-CONTEXT.md
@.planning/phases/05-auth-and-onboarding/05-RESEARCH.md
@.planning/phases/05-auth-and-onboarding/05-VALIDATION.md

@src/lib/supabase.ts
@src/lib/types.ts
@src/stores/ui.ts
@src/stores/filters.ts
@supabase/functions/search-trails/index.ts
@supabase/functions/_shared/cors.ts
@supabase/migrations/20260308210000_initial_schema.sql
@vitest.config.ts

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/lib/types.ts:
```typescript
export interface User {
  id: string
  display_name: string | null
  dog_name: string | null
  avatar_url: string | null
  created_at: string
}

export interface Invitation {
  id: string
  token: string
  created_by: string
  used_by: string | null
  used_at: string | null
  expires_at: string
  created_at: string
}
```

From src/lib/supabase.ts:
```typescript
export const supabase = createClient(supabaseUrl ?? '', supabaseAnonKey ?? '')
```

From src/stores/ui.ts (pattern to follow):
```typescript
export const useUIStore = create<UIState>((set) => ({ ... }))
```

From supabase/functions/_shared/cors.ts:
```typescript
export const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 0 test scaffolding for all phase 5 features</name>
  <files>vitest.config.ts, src/stores/auth.test.ts, src/features/auth/validateInvite.test.ts, src/features/auth/InvitePage.test.tsx, src/features/auth/RegisterForm.test.tsx, src/features/auth/AuthPage.test.tsx, src/components/ui/BottomTabBar.test.tsx, src/features/onboarding/OnboardingFlow.test.tsx, src/features/onboarding/DogStep.test.tsx, src/features/onboarding/GeolocationStep.test.tsx</files>
  <action>
1. **Update vitest.config.ts** — Change `include` from `['src/**/*.test.ts']` to `['src/**/*.test.{ts,tsx}']` so component tests (.tsx) are picked up.

2. **Create 9 Wave 0 test stub files.** Each file should contain a `describe` block with the feature name and `it.todo()` placeholders for the behaviors that will be tested when the feature is implemented in later tasks/plans. Use `import { describe, it } from 'vitest'` in each file. The stubs pass because `it.todo()` tests are skipped, not failed.

**src/stores/auth.test.ts** (AUTH-05, AUTH-06):
```typescript
import { describe, it } from 'vitest'

describe('useAuthStore', () => {
  it.todo('initializes with null session and loading true')
  it.todo('setSession updates session and extracts user')
  it.todo('setProfile stores app user profile')
  it.todo('clear resets all auth state to null')
  it.todo('setInitialized marks store as ready')
})
```

**src/features/auth/validateInvite.test.ts** (AUTH-07):
```typescript
import { describe, it } from 'vitest'

describe('validate-invite logic', () => {
  it.todo('returns valid:true for unexpired, unused token')
  it.todo('returns reason:expired for expired token')
  it.todo('returns reason:used for already-used token')
  it.todo('returns reason:not_found for unknown token')
})
```

**src/features/auth/InvitePage.test.tsx** (AUTH-01, AUTH-03):
```typescript
import { describe, it } from 'vitest'

describe('InvitePage', () => {
  it.todo('shows loading spinner during token validation')
  it.todo('renders RegisterForm when token is valid')
  it.todo('shows Polish error for expired token')
  it.todo('shows Polish error for used token')
  it.todo('shows Polish error for not_found token')
  it.todo('shows "Dostep tylko przez zaproszenie" when no token param')
})
```

**src/features/auth/RegisterForm.test.tsx** (AUTH-02):
```typescript
import { describe, it } from 'vitest'

describe('RegisterForm', () => {
  it.todo('renders name and email fields')
  it.todo('submit button is disabled when fields are empty')
  it.todo('calls signInWithOtp on valid submission')
  it.todo('shows MagicLinkSent on successful OTP send')
  it.todo('shows Polish error on failed OTP send')
})
```

**src/features/auth/AuthPage.test.tsx** (AUTH-04):
```typescript
import { describe, it } from 'vitest'

describe('AuthPage', () => {
  it.todo('shows invite-only gate when no from=invite param')
  it.todo('shows email login form for returning users')
  it.todo('calls signInWithOtp with shouldCreateUser:false')
})
```

**src/components/ui/BottomTabBar.test.tsx** (AUTH-08):
```typescript
import { describe, it } from 'vitest'

describe('BottomTabBar auth interception', () => {
  it.todo('navigates normally for Mapa and Trasy tabs')
  it.todo('shows AuthGateSheet when tapping Ulubione while unauthenticated')
  it.todo('shows AuthGateSheet when tapping Profil while unauthenticated')
  it.todo('navigates normally when authenticated')
})
```

**src/features/onboarding/OnboardingFlow.test.tsx** (ONBR-01, ONBR-04):
```typescript
import { describe, it } from 'vitest'

describe('OnboardingFlow', () => {
  it.todo('renders 4 progress dots')
  it.todo('starts at step 1 (WelcomeStep)')
  it.todo('advances through all 4 steps')
  it.todo('sets showFilterTooltip on completion')
  it.todo('navigates to / after final step')
})
```

**src/features/onboarding/DogStep.test.tsx** (ONBR-02):
```typescript
import { describe, it } from 'vitest'

describe('DogStep', () => {
  it.todo('requires dog name before advancing')
  it.todo('saves dog_name to database on submit')
  it.todo('updates auth store profile locally')
  it.todo('breed field is optional')
})
```

**src/features/onboarding/GeolocationStep.test.tsx** (ONBR-03):
```typescript
import { describe, it } from 'vitest'

describe('GeolocationStep', () => {
  it.todo('shows GPS permission explanation')
  it.todo('requests geolocation on button tap')
  it.todo('advances on GPS success')
  it.todo('shows skip option on GPS denial')
})
```
  </action>
  <verify>
    <automated>npx vitest run src/stores/auth.test.ts src/features/auth src/features/onboarding src/components/ui/BottomTabBar.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <done>Vitest config includes .tsx files. All 9 Wave 0 test files exist with it.todo() stubs covering all phase 5 requirements. `npx vitest run` finds and passes all test files (todos are skipped, not failed).</done>
</task>

<task type="auto">
  <name>Task 1: Auth store, types update</name>
  <files>src/stores/auth.ts, src/lib/types.ts</files>
  <action>
1. **Update src/lib/types.ts** — Add `walk_preferences` field to User interface:
```typescript
export interface User {
  id: string
  display_name: string | null
  dog_name: string | null
  avatar_url: string | null
  walk_preferences: WalkPreferences | null
  created_at: string
}

export interface WalkPreferences {
  length: 'short' | 'medium' | 'long' | null
  water: 'required' | 'preferred' | 'any'
  surface: SurfaceType | null
}
```

2. **Create src/stores/auth.ts** — Zustand auth store following existing store patterns (see ui.ts, filters.ts):
```typescript
interface AuthState {
  session: Session | null
  user: SupabaseUser | null  // from @supabase/supabase-js
  profile: AppUser | null     // from lib/types.ts User
  loading: boolean
  initialized: boolean        // true after first getSession completes
  setSession: (session: Session | null) => void
  setProfile: (profile: AppUser | null) => void
  setLoading: (loading: boolean) => void
  setInitialized: () => void
  clear: () => void
}
```

Import `Session` and `User as SupabaseUser` from `@supabase/supabase-js`. Import `User as AppUser` from `../lib/types`. Export `useAuthStore`.

The `setSession` setter should also set `user: session?.user ?? null`. The `clear` action resets session, user, profile to null.

Do NOT put the auth init logic (getSession, onAuthStateChange) in the store — that goes in App.tsx (Plan 03). The store is purely state + setters.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run src/stores/auth.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Auth store exports useAuthStore with session/user/profile/loading/initialized. User type has walk_preferences. Build passes. Auth store test file runs (todos skipped).</done>
</task>

<task type="auto">
  <name>Task 2: DB migration and validate-invite Edge Function</name>
  <files>supabase/migrations/20260314000000_auth_support.sql, supabase/functions/validate-invite/index.ts</files>
  <action>
1. **Create supabase/migrations/20260314000000_auth_support.sql** with these changes:

```sql
-- Add walk_preferences JSONB column to users
ALTER TABLE public.users
  ADD COLUMN IF NOT EXISTS walk_preferences jsonb;

-- Trigger: auto-create public.users row when auth.users row is created
-- display_name comes from raw_user_meta_data set during signInWithOtp
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS trigger AS $$
BEGIN
  INSERT INTO public.users (id, display_name)
  VALUES (new.id, new.raw_user_meta_data ->> 'display_name')
  ON CONFLICT (id) DO NOTHING;
  RETURN new;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Drop if exists to make idempotent
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- SECURITY DEFINER function to consume invite tokens
-- Called via supabase.rpc('consume_invite', { p_token, p_user_id })
-- Bypasses RLS so the newly-authenticated user can mark token as used
CREATE OR REPLACE FUNCTION public.consume_invite(p_token text, p_user_id uuid)
RETURNS void AS $$
BEGIN
  UPDATE public.invitations
  SET used_by = p_user_id, used_at = now()
  WHERE token = p_token AND used_at IS NULL;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

2. **Create supabase/functions/validate-invite/index.ts** — Follow the exact pattern from search-trails:
   - Import `createClient` from `jsr:@supabase/supabase-js@2`
   - Import `corsHeaders` from `../_shared/cors.ts`
   - Handle OPTIONS preflight with corsHeaders
   - Accept POST with `{ token: string }` body
   - Create service role client using `Deno.env.get('SUPABASE_URL')` and `Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')`
   - Query `invitations` table: `.select('id, expires_at, used_at, used_by').eq('token', token).single()`
   - Return JSON responses with corsHeaders + Content-Type:
     - Token not found: `{ valid: false, reason: 'not_found' }` (404)
     - Token expired (`expires_at < now()`): `{ valid: false, reason: 'expired' }` (410)
     - Token already used (`used_at !== null`): `{ valid: false, reason: 'used' }` (410)
     - Valid token: `{ valid: true, invitation_id: data.id }` (200)
   - Wrap in try/catch, return 500 on unexpected errors
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run src/features/auth/validateInvite.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Migration file exists with walk_preferences column, handle_new_user trigger, and consume_invite function. validate-invite Edge Function exists and follows search-trails pattern. Build passes (Edge Function is Deno, not checked by Vite build, but migration and types are consistent). validateInvite test runs (todos skipped).</done>
</task>

</tasks>

<verification>
- `npm run build` passes (TypeScript compilation succeeds with updated types)
- `npx vitest run` passes (all Wave 0 test stubs pass with skipped todos)
- `src/stores/auth.ts` exports `useAuthStore`
- `src/lib/types.ts` has `WalkPreferences` interface and `walk_preferences` on User
- `supabase/functions/validate-invite/index.ts` exists and follows cors/service-role pattern
- `supabase/migrations/20260314000000_auth_support.sql` has trigger + function + column
- All 9 Wave 0 test files exist with it.todo() stubs
</verification>

<success_criteria>
- Auth store is importable and provides session/user/profile/loading/initialized state
- User type includes walk_preferences field
- validate-invite Edge Function handles all 4 token states (not_found, expired, used, valid)
- DB migration adds walk_preferences column, handle_new_user trigger, consume_invite RPC
- Vitest includes .tsx test files and all Wave 0 stubs pass
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-auth-and-onboarding/05-01-SUMMARY.md`
</output>
