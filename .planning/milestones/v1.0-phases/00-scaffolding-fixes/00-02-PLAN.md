---
phase: 00-scaffolding-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - vite.config.ts
  - docs/PRD.md
autonomous: true
requirements: [FOUN-03, FOUN-04, FOUN-05, FOUN-06]

must_haves:
  truths:
    - "No CSS rule hides Mapbox attribution — ToS compliance restored"
    - "Workbox caches opaque tile responses without quota exhaustion"
    - "dist/ is not tracked in git"
    - "PRD schema block uses 'moderate' and 'geometry' matching actual migration"
  artifacts:
    - path: "src/index.css"
      provides: "Clean CSS without attribution override"
      min_lines: 50
    - path: "vite.config.ts"
      provides: "Corrected Workbox runtimeCaching config"
      contains: "psi-szlak-mapbox-tiles"
    - path: "docs/PRD.md"
      provides: "PRD with correct column names"
      contains: "moderate"
  key_links:
    - from: "vite.config.ts"
      to: "Workbox service worker"
      via: "VitePWA plugin runtimeCaching"
      pattern: "cacheableResponse.*statuses.*0.*200"
---

<objective>
Fix four independent scaffolding defects: remove ToS-violating Mapbox CSS override, correct Workbox tile cache config, verify dist/ git status, and update PRD column names.

Purpose: The CSS override violates Mapbox ToS (risk of token revocation). The Workbox config silently fails to cache opaque tile responses and can exhaust storage quota. The PRD inaccuracies will mislead future Edge Function development.

Output: Fixed src/index.css, vite.config.ts, docs/PRD.md. Verified dist/ git state.
</objective>

<execution_context>
@/Users/kacpermajerowicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kacpermajerowicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-scaffolding-fixes/00-RESEARCH.md

<interfaces>
<!-- Current vite.config.ts runtimeCaching (to be fixed) -->
From vite.config.ts:
```typescript
runtimeCaching: [
  {
    urlPattern: /^https:\/\/api\.mapbox\.com\/.*/i,
    handler: 'CacheFirst',
    options: {
      cacheName: 'mapbox-tiles',                        // WRONG: rename to psi-szlak-mapbox-tiles
      expiration: { maxEntries: 500, maxAgeSeconds: 60 * 60 * 24 * 7 },
      // MISSING: cacheableResponse: { statuses: [0, 200] }
      // MISSING: purgeOnQuotaError: true
    },
  },
],
```

<!-- Current src/index.css lines 65-68 (DELETE) -->
From src/index.css:
```css
/* -- Mapbox overrides -- */
.mapboxgl-ctrl-attrib {
  display: none !important;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Mapbox CSS override and fix Workbox config</name>
  <files>src/index.css, vite.config.ts</files>
  <action>
    1. Edit `src/index.css`: Delete lines 65-68 entirely (the `/* -- Mapbox overrides -- */` comment and the `.mapboxgl-ctrl-attrib { display: none !important; }` rule). The file should end after the `body` block at line 63. Do NOT add any replacement — the compact attribution will be configured in Phase 1 when the MapView component is built.

    2. Edit `vite.config.ts` runtimeCaching entry:
       - Change `cacheName` from `'mapbox-tiles'` to `'psi-szlak-mapbox-tiles'`
       - Add `cacheableResponse: { statuses: [0, 200] }` to `options`
       - Add `purgeOnQuotaError: true` to `options`
       - Keep existing `urlPattern`, `handler`, and `expiration` unchanged

    The corrected options block should be:
    ```typescript
    options: {
      cacheName: 'psi-szlak-mapbox-tiles',
      expiration: { maxEntries: 500, maxAgeSeconds: 60 * 60 * 24 * 7 },
      cacheableResponse: { statuses: [0, 200] },
      purgeOnQuotaError: true,
    },
    ```
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && grep -c "mapboxgl-ctrl-attrib" src/index.css; grep -c "psi-szlak-mapbox-tiles" vite.config.ts; grep -c "cacheableResponse" vite.config.ts; grep -c "purgeOnQuotaError" vite.config.ts</automated>
  </verify>
  <done>grep for "mapboxgl-ctrl-attrib" in src/index.css returns 0. grep for "psi-szlak-mapbox-tiles" returns 1. grep for "cacheableResponse" returns 1. grep for "purgeOnQuotaError" returns 1.</done>
</task>

<task type="auto">
  <name>Task 2: Verify dist/ git state and fix PRD column names</name>
  <files>docs/PRD.md</files>
  <action>
    1. Verify dist/ is not tracked: Run `git ls-files dist/` — research confirmed it returns empty. If somehow it IS tracked, run `git rm -r --cached dist/`. Verify `.gitignore` contains `dist` entry. Document the finding.

    2. Edit `docs/PRD.md` — find and replace these specific terms in the section 4 schema block and any other occurrences:
       - Replace `"medium"` with `"moderate"` in the difficulty enum description (line ~95: `"easy" | "medium" | "hard"` becomes `"easy" | "moderate" | "hard"`)
       - Replace `medium` with `moderate` in the sac_scale mapping text (line ~106: `mountain_hiking → medium` becomes `mountain_hiking → moderate`)
       - Replace `medium` with `moderate` in the SQL-style schema block (line ~154: `easy / medium / hard` becomes `easy / moderate / hard`)
       - Replace `geojson` with `geometry` in the SQL-style schema block (line ~159: `geojson         jsonb` becomes `geometry        jsonb`)

    Be careful to only change these specific occurrences. Do NOT change any other text in PRD.md.
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && git ls-files dist/ | wc -l; grep -c "medium" docs/PRD.md; grep -c "geojson" docs/PRD.md</automated>
  </verify>
  <done>git ls-files dist/ returns 0 lines. grep for "medium" in PRD returns 0. grep for "geojson" in PRD returns 0. All references now use "moderate" and "geometry".</done>
</task>

</tasks>

<verification>
- `grep -r "mapboxgl-ctrl-attrib" src/` returns empty
- `grep "psi-szlak-mapbox-tiles" vite.config.ts` returns 1 match
- `grep "cacheableResponse" vite.config.ts` returns 1 match
- `grep "purgeOnQuotaError" vite.config.ts` returns 1 match
- `git ls-files dist/` returns empty
- `grep "medium\|geojson" docs/PRD.md` returns empty (no old column names remain)
- `npm run build` succeeds (vite.config.ts is still valid)
</verification>

<success_criteria>
- No Mapbox attribution CSS override exists anywhere in src/
- Workbox runtimeCaching has all 3 fixes: cache name, cacheableResponse, purgeOnQuotaError
- dist/ confirmed not tracked in git
- PRD uses "moderate" (not "medium") and "geometry" (not "geojson") everywhere
- `npm run build` still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/00-scaffolding-fixes/00-02-SUMMARY.md`
</output>
