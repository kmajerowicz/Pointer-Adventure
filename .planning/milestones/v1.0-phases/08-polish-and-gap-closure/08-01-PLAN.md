---
phase: 08-polish-and-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ToastRenderer.tsx
  - src/components/layout/AuthLayout.tsx
  - .planning/phases/05-auth-and-onboarding/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [ACT-02, DS-05]
gap_closure: true

must_haves:
  truths:
    - "Tapping Przeszedlem on TrailDetail shows a visible toast that auto-dismisses"
    - "DS-01 through DS-06 traceability checkboxes are marked complete in REQUIREMENTS.md"
    - "Phase 5 VERIFICATION.md status reflects current reality (ONBR-04 resolved)"
  artifacts:
    - path: "src/components/ui/ToastRenderer.tsx"
      provides: "Toast overlay reading useUIStore.toast and rendering with auto-dismiss"
      min_lines: 20
    - path: "src/components/layout/AuthLayout.tsx"
      provides: "Mounts ToastRenderer so toast is visible on all routes"
      contains: "ToastRenderer"
  key_links:
    - from: "src/hooks/useActivity.ts"
      to: "src/stores/ui.ts"
      via: "showToast('Zapisano spacer!')"
      pattern: "showToast"
    - from: "src/components/ui/ToastRenderer.tsx"
      to: "src/stores/ui.ts"
      via: "useUIStore((s) => s.toast)"
      pattern: "useUIStore.*toast"
    - from: "src/components/layout/AuthLayout.tsx"
      to: "src/components/ui/ToastRenderer.tsx"
      via: "renders <ToastRenderer />"
      pattern: "ToastRenderer"
---

<objective>
Close the last two gaps from the v1.0 milestone audit: wire the missing toast renderer so user actions produce visible feedback, and update stale documentation to reflect current reality.

Purpose: ACT-02 is partially broken because `showToast()` writes to UIStore but nothing renders it. DS-05 ("every user action gets feedback") is violated. Phase 5 verification is stale.
Output: Working toast overlay, accurate VERIFICATION.md, complete DS traceability.
</objective>

<execution_context>
@/Users/kacpermajerowicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kacpermajerowicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Toast infrastructure already exists in ui.ts — executor needs these contracts -->

From src/stores/ui.ts:
```typescript
export interface ToastMessage {
  id: number
  message: string
}

// Relevant UIState members:
toast: ToastMessage | null
showToast: (message: string) => void
clearToast: () => void
```

From src/components/layout/AuthLayout.tsx:
```typescript
// Root route wrapper — renders <OfflineBanner /> + <Outlet />
// ToastRenderer must be added here (not AppLayout) so it covers
// standalone routes like TrailDetail
export function AuthLayout(): JSX.Element
```

From src/hooks/useActivity.ts:
```typescript
// Line 57: showToast('Zapisano spacer!') — this is the primary caller
// Line 59: showToast('Nie udalo sie zapisac spaceru') — error case
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToastRenderer component and mount in AuthLayout</name>
  <files>src/components/ui/ToastRenderer.tsx, src/components/layout/AuthLayout.tsx</files>
  <action>
Create `src/components/ui/ToastRenderer.tsx`:
- Subscribe to `useUIStore((s) => s.toast)` and `useUIStore((s) => s.clearToast)`
- When `toast` is non-null, render a fixed overlay positioned at the bottom of the viewport (above tab bar: `bottom: calc(var(--spacing-tab-bar) + 1rem)`)
- Use design tokens: `bg-bg-elevated` background, `text-text-primary` text, `rounded-xl` corners, `px-4 py-3` padding, subtle shadow
- Auto-dismiss after 3 seconds using `useEffect` with `setTimeout` that calls `clearToast()` — keyed on `toast?.id` so each new toast resets the timer
- Clean up timeout on unmount or toast change
- Add a fade-in entrance via Tailwind `animate-` class or inline transition (opacity 0 to 1)
- Center horizontally with `left-1/2 -translate-x-1/2` and set `z-50` to stay above all content
- Component renders `null` when `toast` is null

Update `src/components/layout/AuthLayout.tsx`:
- Import `ToastRenderer` from `../ui/ToastRenderer`
- Render `<ToastRenderer />` inside the returned fragment, after `<OfflineBanner />` and before `<Outlet />`
- This ensures toast is visible on ALL routes including standalone pages like TrailDetail
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Toast "Zapisano spacer!" renders as a visible overlay when showToast is called from useActivity, auto-dismisses after 3s, positioned above tab bar using design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale Phase 5 verification and mark DS traceability complete</name>
  <files>.planning/phases/05-auth-and-onboarding/05-VERIFICATION.md, .planning/REQUIREMENTS.md</files>
  <action>
Update `.planning/phases/05-auth-and-onboarding/05-VERIFICATION.md`:
- Change frontmatter `status: gaps_found` to `status: passed`
- Change frontmatter `score: 10/11 must-haves verified` to `score: 11/11 must-haves verified`
- Remove the `gaps:` block from frontmatter entirely (no gaps remain)
- In the Observable Truths table row #11 (FilterTooltip), change Status from `FAILED` to `VERIFIED` and update Evidence to: "FilterTooltip is imported and rendered in AppLayout.tsx (line 3: import, line 15: render). Confirmed wired by v1.0 milestone audit."
- In the Key Link Verification table, change FilterTooltip row status from `NOT_WIRED` to `WIRED` and update Details to: "FilterTooltip imported and rendered in AppLayout.tsx"
- In the Required Artifacts table, change FilterTooltip row from `ORPHANED` to `VERIFIED` and update Details to: "Imported and rendered in AppLayout.tsx"
- Update ONBR-04 in Requirements Coverage table from `BLOCKED` to `SATISFIED` with evidence: "FilterTooltip rendered in AppLayout.tsx, reads UIStore showFilterTooltip flag set by OnboardingFlow"
- Remove the Anti-Patterns entry for orphaned FilterTooltip
- Update the Gaps Summary section to state: "No gaps remain. All 11 truths verified."

Update `.planning/REQUIREMENTS.md`:
- Change DS-01 through DS-06 from `- [ ]` to `- [x]` (all confirmed wired by v1.0 audit)
- In the Traceability table at the bottom, change DS-01 through DS-06 Status from `Pending` to `Complete`
  </action>
  <verify>
    <automated>cd /Users/kacpermajerowicz/pointer-adventure && grep -c "status: passed" .planning/phases/05-auth-and-onboarding/05-VERIFICATION.md && grep -c "\- \[x\] \*\*DS-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>05-VERIFICATION.md shows status: passed with 11/11 truths, ONBR-04 marked SATISFIED. REQUIREMENTS.md shows DS-01 through DS-06 all checked complete with traceability updated.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `grep -n "useUIStore" src/components/ui/ToastRenderer.tsx` shows toast state subscription
3. `grep -n "ToastRenderer" src/components/layout/AuthLayout.tsx` shows mount point
4. `grep "status: passed" .planning/phases/05-auth-and-onboarding/05-VERIFICATION.md` confirms fix
5. `grep -c "\[x\] \*\*DS-0" .planning/REQUIREMENTS.md` returns 6 (all DS requirements checked)
</verification>

<success_criteria>
- ToastRenderer component exists, reads UIStore toast state, renders fixed overlay with auto-dismiss
- AuthLayout mounts ToastRenderer so all routes can display toasts
- Build passes with no errors
- 05-VERIFICATION.md status is "passed" with 11/11 score and no gaps
- REQUIREMENTS.md DS-01 through DS-06 all marked [x] Complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-polish-and-gap-closure/08-01-SUMMARY.md`
</output>
