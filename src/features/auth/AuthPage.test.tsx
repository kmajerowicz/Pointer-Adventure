import { describe, it } from 'vitest'

describe('AuthPage', () => {
  it.todo('shows invite-only gate when no from=invite param')
  it.todo('shows email login form for returning users')
  it.todo('calls signInWithOtp with shouldCreateUser:false')
})
